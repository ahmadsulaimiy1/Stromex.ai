<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only in force for the instant between the window opening and Compose drawing its first
         frame. windowBackground matches the Compose surface colour so there is no flash. -->
    <style name="Theme.Sajjil" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
